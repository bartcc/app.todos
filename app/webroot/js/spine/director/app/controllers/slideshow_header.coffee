Spine       = require("spine")
$           = Spine.$

class SlideshowHeader extends Spine.Controller
  
  template: (item) ->

  constructor: ->
    super

  render: ->
    
  activated: ->
    @render()
    
module?.exports = SlideshowHeader